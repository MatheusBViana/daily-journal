<%- include("partials/header") %>
<h1>Home</h1>
<p> <%= content %></p>

        <% new_post.forEach(function(post) { %>
        <a href="/posts/<%= post.title %>"><h2> <%= post.title %> </h2>
        <p><%= post.content.substring(0, 80) + "..." %> </p>
        </a>
        
        <hr>
        <% }) %>

<%- include("partials/footer") %>
